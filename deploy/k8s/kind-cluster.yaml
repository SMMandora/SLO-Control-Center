kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: slo
nodes:
  - role: control-plane
    extraPortMappings:
      # frontend NodePort -> localhost:30000
      - containerPort: 30000
        hostPort: 30000
        protocol: TCP
      # grafana NodePort -> localhost:30001
      - containerPort: 30001
        hostPort: 30001
        protocol: TCP
